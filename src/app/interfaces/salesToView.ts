export interface SalesToView {
  id: string;
  client: string;
  product: string;
  quantity: number;
  payMethod: string;
  phone: string;
  address: string;
  email: string;
  state: string;
  price: number;
  dateCreated: Date;
}
